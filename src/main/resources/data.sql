CREATE TABLE BUILDINGS (
    `base_cost` int,
    `icon_frame` int,
    `infrastructure` int,
    `max_level` int,
    `value` int,
    `show_modifier` int,
    `max_fuel_building` int,
    `allied_build` int,
    `infrastruture_construction_effect` int
);